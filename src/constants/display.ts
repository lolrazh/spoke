export const REFERENCE_WIDTH = 1728; // logical px reference for scaling
export const MIN_UI_SCALE = 0.9;
export const MAX_UI_SCALE = 1.0;

// Polling interval (5 Hz) for following the cursor/display changes. Polling is
// paused entirely while the pill window is hidden.
export const CURSOR_POLL_INTERVAL_MS = 200;
